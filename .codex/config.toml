[windows]
sandbox = "elevated"

[agents]
enabled = true
max_concurrent_threads_per_session = 6

# Safety fallback for any unnamed subagent.
default_subagent_model = "gpt-5.6-luna"
default_subagent_reasoning_effort = "max"

interrupt_message = true
