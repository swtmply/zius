executionPrerequisite: Android app open on the home onboarding first spotlight, Your balance at a glance.
steps:
  - echo: Advance spotlight to open expenses.
  - tap: { text: Next spotlight }
  - await: { visible: { text: Keep track of open expenses } }
  - await: { idle: true }
  - echo: Advance spotlight to settled history.
  - tap: { text: Next spotlight }
  - await: { visible: { text: Your settled history } }
  - await: { idle: true }
  - echo: Advance spotlight to receipt scanning.
  - tap: { text: Next spotlight }
  - await: { visible: { text: Scan a receipt } }
  - await: { idle: true }
