import { HugeiconsIcon } from "@hugeicons/react-native";
import { withUniwind } from "uniwind";

/** HugeiconsIcon with a theme-aware color: <Icon icon={X} colorClassName="accent-ink" /> */
export const Icon = withUniwind(HugeiconsIcon);
